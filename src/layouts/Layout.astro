---
// import { ViewTransitions } from 'astro:transitions';
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="BelkaPay - белорусская компания, специализирующаяся на разработке и поставке платежных решений по модели White label." />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="./favicon.svg" />
    <script></script>
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <!-- <ViewTransitions /> -->
  </head>
  <body>
    <slot />
  </body>
</html>
<style lang="scss" is:global>
  @import '../styles/fonts';

  :root {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
    --accent-gradient: linear-gradient(45deg, rgb(var(--accent)), rgb(var(--accent-light)) 30%, white 60%);
  }
  html {
    background: #eff1f4;
    color: #1f2d45;
    font-style: normal;
    font-weight: 400;
    font-size: 18px;
    line-height: 24px;
    font-family: 'Proxima Nova', sans-serif;
  }
  li {
    list-style-type: initial;
  }
  .title {
    font-weight: 800;
    font-size: 30px;
    line-height: 40px;
    @include media('>=tablet') {
      font-size: 40px;
      line-height: 47px;
    }
  }
  .title-main {
    font-weight: 800;
    font-size: 40px;
    line-height: 48px;
    @include media('>=desktop') {
      font-size: 64px;
      line-height: 72px;
    }
  }
</style>